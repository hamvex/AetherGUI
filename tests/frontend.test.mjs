import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('frontend exposes required controls',async()=>{const html=await readFile(new URL('../src/index.html',import.meta.url),'utf8');for(const id of ['connect','disconnect','test','protocol','scanMode','ipMode','obfuscation','transport','logs','copyLogs','clearLogs'])assert.match(html,new RegExp(`id="${id}"`));});
test('frontend contains no remote scripts',async()=>{const html=await readFile(new URL('../src/index.html',import.meta.url),'utf8');assert.doesNotMatch(html,/src="https?:/);});
test('application and bundled core versions stay aligned',async()=>{const [pkg,tauri,cargo,fetch]=await Promise.all([readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../src-tauri/tauri.conf.json',import.meta.url),'utf8'),readFile(new URL('../src-tauri/Cargo.toml',import.meta.url),'utf8'),readFile(new URL('../scripts/fetch-aether.ps1',import.meta.url),'utf8')]);assert.equal(JSON.parse(pkg).version,'1.2.0');assert.equal(JSON.parse(tauri).version,'1.2.0');assert.match(cargo,/version = "1\.2\.0"/);assert.match(fetch,/"v1\.2\.0"/);});
